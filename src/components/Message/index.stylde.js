import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 10px;
  display: flex;
  justify-content: center;
  font-size: calc(16px + (30 - 16) * ((100vw - 390px) / (1920 - 390)));
  color: #151515;
  font-family: Inter;
`;
